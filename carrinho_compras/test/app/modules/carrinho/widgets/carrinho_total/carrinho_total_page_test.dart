import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CarrinhoTotalPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarrinhoTotalPage(title: 'CarrinhoTotal')));
    //  final titleFinder = find.text('CarrinhoTotal');
    //  expect(titleFinder, findsOneWidget);
  });
}
