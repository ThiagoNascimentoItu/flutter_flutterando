import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CarrinhoListaProdutosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarrinhoListaProdutosPage(title: 'CarrinhoListaProdutos')));
    //  final titleFinder = find.text('CarrinhoListaProdutos');
    //  expect(titleFinder, findsOneWidget);
  });
}
